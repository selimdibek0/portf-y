# Admin panel giriş şifresi (en az 8 karakter) — ZORUNLU (Render)
ADMIN_PASSWORD=guclu_sifreniz

# Render otomatik atar — yerelde 3001 kullanın, Render'da ELLE EKLEMEYİN
PORT=3001

# Render'da otomatik production olur
NODE_ENV=production
