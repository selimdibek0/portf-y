{
  "name": "cv-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "predev": "node scripts/free-ports.mjs",
    "dev": "concurrently -n api,vite -c blue,green \"npm run server\" \"vite\"",
    "server": "node server/index.js",
    "build": "vite build",
    "preview": "concurrently -n api,vite -c blue,green \"npm run server\" \"vite preview\"",
    "start": "node server/index.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "multer": "^2.0.1",
    "vite": "^6.3.5"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
